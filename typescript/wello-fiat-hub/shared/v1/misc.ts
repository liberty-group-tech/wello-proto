// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.32.0
// source: wello-fiat-hub/shared/v1/misc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { DateMessage } from "../../../google/type/date";

export const protobufPackage = "shared.v1";

export enum FilterOp {
  EQ = 0,
  NE = 1,
  GT = 2,
  GTE = 3,
  LT = 4,
  LTE = 5,
  IN = 6,
  OR = 7,
  AND = 8,
  /** CONTAINS - more ops */
  CONTAINS = 9,
  UNRECOGNIZED = -1,
}

export function filterOpFromJSON(object: any): FilterOp {
  switch (object) {
    case 0:
    case "EQ":
      return FilterOp.EQ;
    case 1:
    case "NE":
      return FilterOp.NE;
    case 2:
    case "GT":
      return FilterOp.GT;
    case 3:
    case "GTE":
      return FilterOp.GTE;
    case 4:
    case "LT":
      return FilterOp.LT;
    case 5:
    case "LTE":
      return FilterOp.LTE;
    case 6:
    case "IN":
      return FilterOp.IN;
    case 7:
    case "OR":
      return FilterOp.OR;
    case 8:
    case "AND":
      return FilterOp.AND;
    case 9:
    case "CONTAINS":
      return FilterOp.CONTAINS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FilterOp.UNRECOGNIZED;
  }
}

export function filterOpToJSON(object: FilterOp): string {
  switch (object) {
    case FilterOp.EQ:
      return "EQ";
    case FilterOp.NE:
      return "NE";
    case FilterOp.GT:
      return "GT";
    case FilterOp.GTE:
      return "GTE";
    case FilterOp.LT:
      return "LT";
    case FilterOp.LTE:
      return "LTE";
    case FilterOp.IN:
      return "IN";
    case FilterOp.OR:
      return "OR";
    case FilterOp.AND:
      return "AND";
    case FilterOp.CONTAINS:
      return "CONTAINS";
    case FilterOp.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum Order {
  ascending = 0,
  descending = 1,
  UNRECOGNIZED = -1,
}

export function orderFromJSON(object: any): Order {
  switch (object) {
    case 0:
    case "ascending":
      return Order.ascending;
    case 1:
    case "descending":
      return Order.descending;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Order.UNRECOGNIZED;
  }
}

export function orderToJSON(object: Order): string {
  switch (object) {
    case Order.ascending:
      return "ascending";
    case Order.descending:
      return "descending";
    case Order.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Filter {
  key: string;
  stringValue?: string | undefined;
  numberValue?: number | undefined;
  boolValue?: boolean | undefined;
  stringArrayValue?: Filter_StringArrayValue | undefined;
  numberArrayValue?: Filter_Int64ArrayValue | undefined;
  op?: FilterOp | undefined;
  filters: Filter[];
}

export interface Filter_StringArrayValue {
  values: string[];
}

export interface Filter_Int64ArrayValue {
  values: number[];
}

export interface OrderBy {
  key: string;
  order: Order;
}

export interface Paging {
  skip: number;
  limit: number;
}

export interface Fee {
  name: string;
  currency: string;
  amount: string;
}

export interface Callback {
  successUrl?: string | undefined;
  failureUrl?: string | undefined;
  webhookUrl?: string | undefined;
}

export interface Identity {
  id?: string | undefined;
  refId: string;
  store?: boolean | undefined;
  email?: string | undefined;
  phone?: string | undefined;
  firstName?: string | undefined;
  middleName?: string | undefined;
  lastName?: string | undefined;
  fullName?: string | undefined;
  dateOfBirth?: DateMessage | undefined;
  address?: Address | undefined;
  passport?: Passport | undefined;
  driverLicense?: DriverLicense | undefined;
  idCard?: IDCard | undefined;
}

export interface Passport {
  number: string;
  fullName: string;
  gender: string;
  dateOfBirth: string;
  issuingCountry: string;
  issuingDate: string;
  expiryDate: string;
  assets: string[];
}

export interface DriverLicense {
  number: string;
  fullName: string;
  gender: string;
  dateOfBirth: string;
  issuingCountry: string;
  expiryDate: string;
  frontUrl?: string | undefined;
  backUrl?: string | undefined;
}

export interface IDCard {
  number: string;
  fullName: string;
  gender: string;
  dateOfBirth: string;
  issuingCountry: string;
  expiryDate: string;
  frontUrl?: string | undefined;
  backUrl?: string | undefined;
}

export interface Address {
  line1: string;
  line2?: string | undefined;
  city: string;
  state: string;
  postalCode: string;
  country: string;
}

export interface Fingerprint {
  ip?: string | undefined;
  userAgent?: string | undefined;
  deviceId?: string | undefined;
  deviceOs?: string | undefined;
  deviceType?: string | undefined;
  language?: string | undefined;
  lastLogin?: Date | undefined;
  metadata: { [key: string]: string };
}

export interface Fingerprint_MetadataEntry {
  key: string;
  value: string;
}

function createBaseFilter(): Filter {
  return {
    key: "",
    stringValue: undefined,
    numberValue: undefined,
    boolValue: undefined,
    stringArrayValue: undefined,
    numberArrayValue: undefined,
    op: undefined,
    filters: [],
  };
}

export const Filter: MessageFns<Filter> = {
  encode(message: Filter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.stringValue !== undefined) {
      writer.uint32(18).string(message.stringValue);
    }
    if (message.numberValue !== undefined) {
      writer.uint32(24).int64(message.numberValue);
    }
    if (message.boolValue !== undefined) {
      writer.uint32(32).bool(message.boolValue);
    }
    if (message.stringArrayValue !== undefined) {
      Filter_StringArrayValue.encode(message.stringArrayValue, writer.uint32(42).fork()).join();
    }
    if (message.numberArrayValue !== undefined) {
      Filter_Int64ArrayValue.encode(message.numberArrayValue, writer.uint32(50).fork()).join();
    }
    if (message.op !== undefined) {
      writer.uint32(56).int32(message.op);
    }
    for (const v of message.filters) {
      Filter.encode(v!, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Filter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stringValue = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.numberValue = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.boolValue = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.stringArrayValue = Filter_StringArrayValue.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.numberArrayValue = Filter_Int64ArrayValue.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.op = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.filters.push(Filter.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Filter {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      stringValue: isSet(object.stringValue) ? globalThis.String(object.stringValue) : undefined,
      numberValue: isSet(object.numberValue) ? globalThis.Number(object.numberValue) : undefined,
      boolValue: isSet(object.boolValue) ? globalThis.Boolean(object.boolValue) : undefined,
      stringArrayValue: isSet(object.stringArrayValue)
        ? Filter_StringArrayValue.fromJSON(object.stringArrayValue)
        : undefined,
      numberArrayValue: isSet(object.numberArrayValue)
        ? Filter_Int64ArrayValue.fromJSON(object.numberArrayValue)
        : undefined,
      op: isSet(object.op) ? filterOpFromJSON(object.op) : undefined,
      filters: globalThis.Array.isArray(object?.filters) ? object.filters.map((e: any) => Filter.fromJSON(e)) : [],
    };
  },

  toJSON(message: Filter): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.stringValue !== undefined) {
      obj.stringValue = message.stringValue;
    }
    if (message.numberValue !== undefined) {
      obj.numberValue = Math.round(message.numberValue);
    }
    if (message.boolValue !== undefined) {
      obj.boolValue = message.boolValue;
    }
    if (message.stringArrayValue !== undefined) {
      obj.stringArrayValue = Filter_StringArrayValue.toJSON(message.stringArrayValue);
    }
    if (message.numberArrayValue !== undefined) {
      obj.numberArrayValue = Filter_Int64ArrayValue.toJSON(message.numberArrayValue);
    }
    if (message.op !== undefined) {
      obj.op = filterOpToJSON(message.op);
    }
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => Filter.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Filter>): Filter {
    return Filter.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Filter>): Filter {
    const message = createBaseFilter();
    message.key = object.key ?? "";
    message.stringValue = object.stringValue ?? undefined;
    message.numberValue = object.numberValue ?? undefined;
    message.boolValue = object.boolValue ?? undefined;
    message.stringArrayValue = (object.stringArrayValue !== undefined && object.stringArrayValue !== null)
      ? Filter_StringArrayValue.fromPartial(object.stringArrayValue)
      : undefined;
    message.numberArrayValue = (object.numberArrayValue !== undefined && object.numberArrayValue !== null)
      ? Filter_Int64ArrayValue.fromPartial(object.numberArrayValue)
      : undefined;
    message.op = object.op ?? undefined;
    message.filters = object.filters?.map((e) => Filter.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFilter_StringArrayValue(): Filter_StringArrayValue {
  return { values: [] };
}

export const Filter_StringArrayValue: MessageFns<Filter_StringArrayValue> = {
  encode(message: Filter_StringArrayValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.values) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Filter_StringArrayValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilter_StringArrayValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.values.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Filter_StringArrayValue {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: Filter_StringArrayValue): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values;
    }
    return obj;
  },

  create(base?: DeepPartial<Filter_StringArrayValue>): Filter_StringArrayValue {
    return Filter_StringArrayValue.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Filter_StringArrayValue>): Filter_StringArrayValue {
    const message = createBaseFilter_StringArrayValue();
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseFilter_Int64ArrayValue(): Filter_Int64ArrayValue {
  return { values: [] };
}

export const Filter_Int64ArrayValue: MessageFns<Filter_Int64ArrayValue> = {
  encode(message: Filter_Int64ArrayValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.values) {
      writer.int64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Filter_Int64ArrayValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilter_Int64ArrayValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.values.push(longToNumber(reader.int64()));

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.values.push(longToNumber(reader.int64()));
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Filter_Int64ArrayValue {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: Filter_Int64ArrayValue): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Filter_Int64ArrayValue>): Filter_Int64ArrayValue {
    return Filter_Int64ArrayValue.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Filter_Int64ArrayValue>): Filter_Int64ArrayValue {
    const message = createBaseFilter_Int64ArrayValue();
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseOrderBy(): OrderBy {
  return { key: "", order: 0 };
}

export const OrderBy: MessageFns<OrderBy> = {
  encode(message: OrderBy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.order !== 0) {
      writer.uint32(16).int32(message.order);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OrderBy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrderBy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.order = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OrderBy {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      order: isSet(object.order) ? orderFromJSON(object.order) : 0,
    };
  },

  toJSON(message: OrderBy): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.order !== 0) {
      obj.order = orderToJSON(message.order);
    }
    return obj;
  },

  create(base?: DeepPartial<OrderBy>): OrderBy {
    return OrderBy.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OrderBy>): OrderBy {
    const message = createBaseOrderBy();
    message.key = object.key ?? "";
    message.order = object.order ?? 0;
    return message;
  },
};

function createBasePaging(): Paging {
  return { skip: 0, limit: 0 };
}

export const Paging: MessageFns<Paging> = {
  encode(message: Paging, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.skip !== 0) {
      writer.uint32(8).int32(message.skip);
    }
    if (message.limit !== 0) {
      writer.uint32(16).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Paging {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaging();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.skip = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Paging {
    return {
      skip: isSet(object.skip) ? globalThis.Number(object.skip) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: Paging): unknown {
    const obj: any = {};
    if (message.skip !== 0) {
      obj.skip = Math.round(message.skip);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create(base?: DeepPartial<Paging>): Paging {
    return Paging.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Paging>): Paging {
    const message = createBasePaging();
    message.skip = object.skip ?? 0;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseFee(): Fee {
  return { name: "", currency: "", amount: "" };
}

export const Fee: MessageFns<Fee> = {
  encode(message: Fee, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.currency !== "") {
      writer.uint32(18).string(message.currency);
    }
    if (message.amount !== "") {
      writer.uint32(26).string(message.amount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Fee {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFee();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Fee {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
    };
  },

  toJSON(message: Fee): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    return obj;
  },

  create(base?: DeepPartial<Fee>): Fee {
    return Fee.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Fee>): Fee {
    const message = createBaseFee();
    message.name = object.name ?? "";
    message.currency = object.currency ?? "";
    message.amount = object.amount ?? "";
    return message;
  },
};

function createBaseCallback(): Callback {
  return { successUrl: undefined, failureUrl: undefined, webhookUrl: undefined };
}

export const Callback: MessageFns<Callback> = {
  encode(message: Callback, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.successUrl !== undefined) {
      writer.uint32(10).string(message.successUrl);
    }
    if (message.failureUrl !== undefined) {
      writer.uint32(18).string(message.failureUrl);
    }
    if (message.webhookUrl !== undefined) {
      writer.uint32(26).string(message.webhookUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Callback {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallback();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.successUrl = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.failureUrl = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.webhookUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Callback {
    return {
      successUrl: isSet(object.successUrl) ? globalThis.String(object.successUrl) : undefined,
      failureUrl: isSet(object.failureUrl) ? globalThis.String(object.failureUrl) : undefined,
      webhookUrl: isSet(object.webhookUrl) ? globalThis.String(object.webhookUrl) : undefined,
    };
  },

  toJSON(message: Callback): unknown {
    const obj: any = {};
    if (message.successUrl !== undefined) {
      obj.successUrl = message.successUrl;
    }
    if (message.failureUrl !== undefined) {
      obj.failureUrl = message.failureUrl;
    }
    if (message.webhookUrl !== undefined) {
      obj.webhookUrl = message.webhookUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<Callback>): Callback {
    return Callback.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Callback>): Callback {
    const message = createBaseCallback();
    message.successUrl = object.successUrl ?? undefined;
    message.failureUrl = object.failureUrl ?? undefined;
    message.webhookUrl = object.webhookUrl ?? undefined;
    return message;
  },
};

function createBaseIdentity(): Identity {
  return {
    id: undefined,
    refId: "",
    store: undefined,
    email: undefined,
    phone: undefined,
    firstName: undefined,
    middleName: undefined,
    lastName: undefined,
    fullName: undefined,
    dateOfBirth: undefined,
    address: undefined,
    passport: undefined,
    driverLicense: undefined,
    idCard: undefined,
  };
}

export const Identity: MessageFns<Identity> = {
  encode(message: Identity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    if (message.refId !== "") {
      writer.uint32(18).string(message.refId);
    }
    if (message.store !== undefined) {
      writer.uint32(24).bool(message.store);
    }
    if (message.email !== undefined) {
      writer.uint32(34).string(message.email);
    }
    if (message.phone !== undefined) {
      writer.uint32(42).string(message.phone);
    }
    if (message.firstName !== undefined) {
      writer.uint32(50).string(message.firstName);
    }
    if (message.middleName !== undefined) {
      writer.uint32(58).string(message.middleName);
    }
    if (message.lastName !== undefined) {
      writer.uint32(66).string(message.lastName);
    }
    if (message.fullName !== undefined) {
      writer.uint32(74).string(message.fullName);
    }
    if (message.dateOfBirth !== undefined) {
      DateMessage.encode(message.dateOfBirth, writer.uint32(82).fork()).join();
    }
    if (message.address !== undefined) {
      Address.encode(message.address, writer.uint32(90).fork()).join();
    }
    if (message.passport !== undefined) {
      Passport.encode(message.passport, writer.uint32(98).fork()).join();
    }
    if (message.driverLicense !== undefined) {
      DriverLicense.encode(message.driverLicense, writer.uint32(106).fork()).join();
    }
    if (message.idCard !== undefined) {
      IDCard.encode(message.idCard, writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Identity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIdentity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.refId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.store = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.phone = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.firstName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.middleName = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.lastName = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.dateOfBirth = DateMessage.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.address = Address.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.passport = Passport.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.driverLicense = DriverLicense.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.idCard = IDCard.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Identity {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      refId: isSet(object.refId) ? globalThis.String(object.refId) : "",
      store: isSet(object.store) ? globalThis.Boolean(object.store) : undefined,
      email: isSet(object.email) ? globalThis.String(object.email) : undefined,
      phone: isSet(object.phone) ? globalThis.String(object.phone) : undefined,
      firstName: isSet(object.firstName) ? globalThis.String(object.firstName) : undefined,
      middleName: isSet(object.middleName) ? globalThis.String(object.middleName) : undefined,
      lastName: isSet(object.lastName) ? globalThis.String(object.lastName) : undefined,
      fullName: isSet(object.fullName) ? globalThis.String(object.fullName) : undefined,
      dateOfBirth: isSet(object.dateOfBirth) ? DateMessage.fromJSON(object.dateOfBirth) : undefined,
      address: isSet(object.address) ? Address.fromJSON(object.address) : undefined,
      passport: isSet(object.passport) ? Passport.fromJSON(object.passport) : undefined,
      driverLicense: isSet(object.driverLicense) ? DriverLicense.fromJSON(object.driverLicense) : undefined,
      idCard: isSet(object.idCard) ? IDCard.fromJSON(object.idCard) : undefined,
    };
  },

  toJSON(message: Identity): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.refId !== "") {
      obj.refId = message.refId;
    }
    if (message.store !== undefined) {
      obj.store = message.store;
    }
    if (message.email !== undefined) {
      obj.email = message.email;
    }
    if (message.phone !== undefined) {
      obj.phone = message.phone;
    }
    if (message.firstName !== undefined) {
      obj.firstName = message.firstName;
    }
    if (message.middleName !== undefined) {
      obj.middleName = message.middleName;
    }
    if (message.lastName !== undefined) {
      obj.lastName = message.lastName;
    }
    if (message.fullName !== undefined) {
      obj.fullName = message.fullName;
    }
    if (message.dateOfBirth !== undefined) {
      obj.dateOfBirth = DateMessage.toJSON(message.dateOfBirth);
    }
    if (message.address !== undefined) {
      obj.address = Address.toJSON(message.address);
    }
    if (message.passport !== undefined) {
      obj.passport = Passport.toJSON(message.passport);
    }
    if (message.driverLicense !== undefined) {
      obj.driverLicense = DriverLicense.toJSON(message.driverLicense);
    }
    if (message.idCard !== undefined) {
      obj.idCard = IDCard.toJSON(message.idCard);
    }
    return obj;
  },

  create(base?: DeepPartial<Identity>): Identity {
    return Identity.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Identity>): Identity {
    const message = createBaseIdentity();
    message.id = object.id ?? undefined;
    message.refId = object.refId ?? "";
    message.store = object.store ?? undefined;
    message.email = object.email ?? undefined;
    message.phone = object.phone ?? undefined;
    message.firstName = object.firstName ?? undefined;
    message.middleName = object.middleName ?? undefined;
    message.lastName = object.lastName ?? undefined;
    message.fullName = object.fullName ?? undefined;
    message.dateOfBirth = (object.dateOfBirth !== undefined && object.dateOfBirth !== null)
      ? DateMessage.fromPartial(object.dateOfBirth)
      : undefined;
    message.address = (object.address !== undefined && object.address !== null)
      ? Address.fromPartial(object.address)
      : undefined;
    message.passport = (object.passport !== undefined && object.passport !== null)
      ? Passport.fromPartial(object.passport)
      : undefined;
    message.driverLicense = (object.driverLicense !== undefined && object.driverLicense !== null)
      ? DriverLicense.fromPartial(object.driverLicense)
      : undefined;
    message.idCard = (object.idCard !== undefined && object.idCard !== null)
      ? IDCard.fromPartial(object.idCard)
      : undefined;
    return message;
  },
};

function createBasePassport(): Passport {
  return {
    number: "",
    fullName: "",
    gender: "",
    dateOfBirth: "",
    issuingCountry: "",
    issuingDate: "",
    expiryDate: "",
    assets: [],
  };
}

export const Passport: MessageFns<Passport> = {
  encode(message: Passport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.number !== "") {
      writer.uint32(10).string(message.number);
    }
    if (message.fullName !== "") {
      writer.uint32(18).string(message.fullName);
    }
    if (message.gender !== "") {
      writer.uint32(26).string(message.gender);
    }
    if (message.dateOfBirth !== "") {
      writer.uint32(34).string(message.dateOfBirth);
    }
    if (message.issuingCountry !== "") {
      writer.uint32(42).string(message.issuingCountry);
    }
    if (message.issuingDate !== "") {
      writer.uint32(50).string(message.issuingDate);
    }
    if (message.expiryDate !== "") {
      writer.uint32(58).string(message.expiryDate);
    }
    for (const v of message.assets) {
      writer.uint32(66).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Passport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePassport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.number = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gender = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dateOfBirth = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.issuingCountry = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.issuingDate = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.expiryDate = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.assets.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Passport {
    return {
      number: isSet(object.number) ? globalThis.String(object.number) : "",
      fullName: isSet(object.fullName) ? globalThis.String(object.fullName) : "",
      gender: isSet(object.gender) ? globalThis.String(object.gender) : "",
      dateOfBirth: isSet(object.dateOfBirth) ? globalThis.String(object.dateOfBirth) : "",
      issuingCountry: isSet(object.issuingCountry) ? globalThis.String(object.issuingCountry) : "",
      issuingDate: isSet(object.issuingDate) ? globalThis.String(object.issuingDate) : "",
      expiryDate: isSet(object.expiryDate) ? globalThis.String(object.expiryDate) : "",
      assets: globalThis.Array.isArray(object?.assets) ? object.assets.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: Passport): unknown {
    const obj: any = {};
    if (message.number !== "") {
      obj.number = message.number;
    }
    if (message.fullName !== "") {
      obj.fullName = message.fullName;
    }
    if (message.gender !== "") {
      obj.gender = message.gender;
    }
    if (message.dateOfBirth !== "") {
      obj.dateOfBirth = message.dateOfBirth;
    }
    if (message.issuingCountry !== "") {
      obj.issuingCountry = message.issuingCountry;
    }
    if (message.issuingDate !== "") {
      obj.issuingDate = message.issuingDate;
    }
    if (message.expiryDate !== "") {
      obj.expiryDate = message.expiryDate;
    }
    if (message.assets?.length) {
      obj.assets = message.assets;
    }
    return obj;
  },

  create(base?: DeepPartial<Passport>): Passport {
    return Passport.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Passport>): Passport {
    const message = createBasePassport();
    message.number = object.number ?? "";
    message.fullName = object.fullName ?? "";
    message.gender = object.gender ?? "";
    message.dateOfBirth = object.dateOfBirth ?? "";
    message.issuingCountry = object.issuingCountry ?? "";
    message.issuingDate = object.issuingDate ?? "";
    message.expiryDate = object.expiryDate ?? "";
    message.assets = object.assets?.map((e) => e) || [];
    return message;
  },
};

function createBaseDriverLicense(): DriverLicense {
  return {
    number: "",
    fullName: "",
    gender: "",
    dateOfBirth: "",
    issuingCountry: "",
    expiryDate: "",
    frontUrl: undefined,
    backUrl: undefined,
  };
}

export const DriverLicense: MessageFns<DriverLicense> = {
  encode(message: DriverLicense, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.number !== "") {
      writer.uint32(10).string(message.number);
    }
    if (message.fullName !== "") {
      writer.uint32(18).string(message.fullName);
    }
    if (message.gender !== "") {
      writer.uint32(26).string(message.gender);
    }
    if (message.dateOfBirth !== "") {
      writer.uint32(34).string(message.dateOfBirth);
    }
    if (message.issuingCountry !== "") {
      writer.uint32(42).string(message.issuingCountry);
    }
    if (message.expiryDate !== "") {
      writer.uint32(50).string(message.expiryDate);
    }
    if (message.frontUrl !== undefined) {
      writer.uint32(58).string(message.frontUrl);
    }
    if (message.backUrl !== undefined) {
      writer.uint32(66).string(message.backUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DriverLicense {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDriverLicense();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.number = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gender = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dateOfBirth = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.issuingCountry = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expiryDate = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.frontUrl = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.backUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DriverLicense {
    return {
      number: isSet(object.number) ? globalThis.String(object.number) : "",
      fullName: isSet(object.fullName) ? globalThis.String(object.fullName) : "",
      gender: isSet(object.gender) ? globalThis.String(object.gender) : "",
      dateOfBirth: isSet(object.dateOfBirth) ? globalThis.String(object.dateOfBirth) : "",
      issuingCountry: isSet(object.issuingCountry) ? globalThis.String(object.issuingCountry) : "",
      expiryDate: isSet(object.expiryDate) ? globalThis.String(object.expiryDate) : "",
      frontUrl: isSet(object.frontUrl) ? globalThis.String(object.frontUrl) : undefined,
      backUrl: isSet(object.backUrl) ? globalThis.String(object.backUrl) : undefined,
    };
  },

  toJSON(message: DriverLicense): unknown {
    const obj: any = {};
    if (message.number !== "") {
      obj.number = message.number;
    }
    if (message.fullName !== "") {
      obj.fullName = message.fullName;
    }
    if (message.gender !== "") {
      obj.gender = message.gender;
    }
    if (message.dateOfBirth !== "") {
      obj.dateOfBirth = message.dateOfBirth;
    }
    if (message.issuingCountry !== "") {
      obj.issuingCountry = message.issuingCountry;
    }
    if (message.expiryDate !== "") {
      obj.expiryDate = message.expiryDate;
    }
    if (message.frontUrl !== undefined) {
      obj.frontUrl = message.frontUrl;
    }
    if (message.backUrl !== undefined) {
      obj.backUrl = message.backUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<DriverLicense>): DriverLicense {
    return DriverLicense.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DriverLicense>): DriverLicense {
    const message = createBaseDriverLicense();
    message.number = object.number ?? "";
    message.fullName = object.fullName ?? "";
    message.gender = object.gender ?? "";
    message.dateOfBirth = object.dateOfBirth ?? "";
    message.issuingCountry = object.issuingCountry ?? "";
    message.expiryDate = object.expiryDate ?? "";
    message.frontUrl = object.frontUrl ?? undefined;
    message.backUrl = object.backUrl ?? undefined;
    return message;
  },
};

function createBaseIDCard(): IDCard {
  return {
    number: "",
    fullName: "",
    gender: "",
    dateOfBirth: "",
    issuingCountry: "",
    expiryDate: "",
    frontUrl: undefined,
    backUrl: undefined,
  };
}

export const IDCard: MessageFns<IDCard> = {
  encode(message: IDCard, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.number !== "") {
      writer.uint32(10).string(message.number);
    }
    if (message.fullName !== "") {
      writer.uint32(18).string(message.fullName);
    }
    if (message.gender !== "") {
      writer.uint32(26).string(message.gender);
    }
    if (message.dateOfBirth !== "") {
      writer.uint32(34).string(message.dateOfBirth);
    }
    if (message.issuingCountry !== "") {
      writer.uint32(42).string(message.issuingCountry);
    }
    if (message.expiryDate !== "") {
      writer.uint32(50).string(message.expiryDate);
    }
    if (message.frontUrl !== undefined) {
      writer.uint32(58).string(message.frontUrl);
    }
    if (message.backUrl !== undefined) {
      writer.uint32(66).string(message.backUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IDCard {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIDCard();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.number = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gender = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dateOfBirth = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.issuingCountry = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expiryDate = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.frontUrl = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.backUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IDCard {
    return {
      number: isSet(object.number) ? globalThis.String(object.number) : "",
      fullName: isSet(object.fullName) ? globalThis.String(object.fullName) : "",
      gender: isSet(object.gender) ? globalThis.String(object.gender) : "",
      dateOfBirth: isSet(object.dateOfBirth) ? globalThis.String(object.dateOfBirth) : "",
      issuingCountry: isSet(object.issuingCountry) ? globalThis.String(object.issuingCountry) : "",
      expiryDate: isSet(object.expiryDate) ? globalThis.String(object.expiryDate) : "",
      frontUrl: isSet(object.frontUrl) ? globalThis.String(object.frontUrl) : undefined,
      backUrl: isSet(object.backUrl) ? globalThis.String(object.backUrl) : undefined,
    };
  },

  toJSON(message: IDCard): unknown {
    const obj: any = {};
    if (message.number !== "") {
      obj.number = message.number;
    }
    if (message.fullName !== "") {
      obj.fullName = message.fullName;
    }
    if (message.gender !== "") {
      obj.gender = message.gender;
    }
    if (message.dateOfBirth !== "") {
      obj.dateOfBirth = message.dateOfBirth;
    }
    if (message.issuingCountry !== "") {
      obj.issuingCountry = message.issuingCountry;
    }
    if (message.expiryDate !== "") {
      obj.expiryDate = message.expiryDate;
    }
    if (message.frontUrl !== undefined) {
      obj.frontUrl = message.frontUrl;
    }
    if (message.backUrl !== undefined) {
      obj.backUrl = message.backUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<IDCard>): IDCard {
    return IDCard.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IDCard>): IDCard {
    const message = createBaseIDCard();
    message.number = object.number ?? "";
    message.fullName = object.fullName ?? "";
    message.gender = object.gender ?? "";
    message.dateOfBirth = object.dateOfBirth ?? "";
    message.issuingCountry = object.issuingCountry ?? "";
    message.expiryDate = object.expiryDate ?? "";
    message.frontUrl = object.frontUrl ?? undefined;
    message.backUrl = object.backUrl ?? undefined;
    return message;
  },
};

function createBaseAddress(): Address {
  return { line1: "", line2: undefined, city: "", state: "", postalCode: "", country: "" };
}

export const Address: MessageFns<Address> = {
  encode(message: Address, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.line1 !== "") {
      writer.uint32(10).string(message.line1);
    }
    if (message.line2 !== undefined) {
      writer.uint32(18).string(message.line2);
    }
    if (message.city !== "") {
      writer.uint32(26).string(message.city);
    }
    if (message.state !== "") {
      writer.uint32(34).string(message.state);
    }
    if (message.postalCode !== "") {
      writer.uint32(42).string(message.postalCode);
    }
    if (message.country !== "") {
      writer.uint32(50).string(message.country);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Address {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.line1 = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.line2 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.city = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.postalCode = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.country = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Address {
    return {
      line1: isSet(object.line1) ? globalThis.String(object.line1) : "",
      line2: isSet(object.line2) ? globalThis.String(object.line2) : undefined,
      city: isSet(object.city) ? globalThis.String(object.city) : "",
      state: isSet(object.state) ? globalThis.String(object.state) : "",
      postalCode: isSet(object.postalCode) ? globalThis.String(object.postalCode) : "",
      country: isSet(object.country) ? globalThis.String(object.country) : "",
    };
  },

  toJSON(message: Address): unknown {
    const obj: any = {};
    if (message.line1 !== "") {
      obj.line1 = message.line1;
    }
    if (message.line2 !== undefined) {
      obj.line2 = message.line2;
    }
    if (message.city !== "") {
      obj.city = message.city;
    }
    if (message.state !== "") {
      obj.state = message.state;
    }
    if (message.postalCode !== "") {
      obj.postalCode = message.postalCode;
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    return obj;
  },

  create(base?: DeepPartial<Address>): Address {
    return Address.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Address>): Address {
    const message = createBaseAddress();
    message.line1 = object.line1 ?? "";
    message.line2 = object.line2 ?? undefined;
    message.city = object.city ?? "";
    message.state = object.state ?? "";
    message.postalCode = object.postalCode ?? "";
    message.country = object.country ?? "";
    return message;
  },
};

function createBaseFingerprint(): Fingerprint {
  return {
    ip: undefined,
    userAgent: undefined,
    deviceId: undefined,
    deviceOs: undefined,
    deviceType: undefined,
    language: undefined,
    lastLogin: undefined,
    metadata: {},
  };
}

export const Fingerprint: MessageFns<Fingerprint> = {
  encode(message: Fingerprint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ip !== undefined) {
      writer.uint32(10).string(message.ip);
    }
    if (message.userAgent !== undefined) {
      writer.uint32(18).string(message.userAgent);
    }
    if (message.deviceId !== undefined) {
      writer.uint32(26).string(message.deviceId);
    }
    if (message.deviceOs !== undefined) {
      writer.uint32(34).string(message.deviceOs);
    }
    if (message.deviceType !== undefined) {
      writer.uint32(42).string(message.deviceType);
    }
    if (message.language !== undefined) {
      writer.uint32(50).string(message.language);
    }
    if (message.lastLogin !== undefined) {
      Timestamp.encode(toTimestamp(message.lastLogin), writer.uint32(58).fork()).join();
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      Fingerprint_MetadataEntry.encode({ key: key as any, value }, writer.uint32(66).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Fingerprint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFingerprint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ip = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userAgent = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.deviceOs = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.deviceType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.language = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.lastLogin = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          const entry8 = Fingerprint_MetadataEntry.decode(reader, reader.uint32());
          if (entry8.value !== undefined) {
            message.metadata[entry8.key] = entry8.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Fingerprint {
    return {
      ip: isSet(object.ip) ? globalThis.String(object.ip) : undefined,
      userAgent: isSet(object.userAgent) ? globalThis.String(object.userAgent) : undefined,
      deviceId: isSet(object.deviceId) ? globalThis.String(object.deviceId) : undefined,
      deviceOs: isSet(object.deviceOs) ? globalThis.String(object.deviceOs) : undefined,
      deviceType: isSet(object.deviceType) ? globalThis.String(object.deviceType) : undefined,
      language: isSet(object.language) ? globalThis.String(object.language) : undefined,
      lastLogin: isSet(object.lastLogin) ? fromJsonTimestamp(object.lastLogin) : undefined,
      metadata: isObject(object.metadata)
        ? Object.entries(object.metadata).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: Fingerprint): unknown {
    const obj: any = {};
    if (message.ip !== undefined) {
      obj.ip = message.ip;
    }
    if (message.userAgent !== undefined) {
      obj.userAgent = message.userAgent;
    }
    if (message.deviceId !== undefined) {
      obj.deviceId = message.deviceId;
    }
    if (message.deviceOs !== undefined) {
      obj.deviceOs = message.deviceOs;
    }
    if (message.deviceType !== undefined) {
      obj.deviceType = message.deviceType;
    }
    if (message.language !== undefined) {
      obj.language = message.language;
    }
    if (message.lastLogin !== undefined) {
      obj.lastLogin = message.lastLogin.toISOString();
    }
    if (message.metadata) {
      const entries = Object.entries(message.metadata);
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<Fingerprint>): Fingerprint {
    return Fingerprint.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Fingerprint>): Fingerprint {
    const message = createBaseFingerprint();
    message.ip = object.ip ?? undefined;
    message.userAgent = object.userAgent ?? undefined;
    message.deviceId = object.deviceId ?? undefined;
    message.deviceOs = object.deviceOs ?? undefined;
    message.deviceType = object.deviceType ?? undefined;
    message.language = object.language ?? undefined;
    message.lastLogin = object.lastLogin ?? undefined;
    message.metadata = Object.entries(object.metadata ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseFingerprint_MetadataEntry(): Fingerprint_MetadataEntry {
  return { key: "", value: "" };
}

export const Fingerprint_MetadataEntry: MessageFns<Fingerprint_MetadataEntry> = {
  encode(message: Fingerprint_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Fingerprint_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFingerprint_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Fingerprint_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Fingerprint_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<Fingerprint_MetadataEntry>): Fingerprint_MetadataEntry {
    return Fingerprint_MetadataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Fingerprint_MetadataEntry>): Fingerprint_MetadataEntry {
    const message = createBaseFingerprint_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
