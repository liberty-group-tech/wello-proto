// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.32.0
// source: wello-hedging/v1/hedging.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "hedging.v1";

export interface QuoteRequest {
  intentId?: string | undefined;
  amount: string;
  baseCurrency: string;
  quoteCurrency: string;
  side: string;
  direction: string;
  type?: string | undefined;
  network: string;
  paymentMethod: string;
  provider: string;
}

export interface Quote {
  quoteId: string;
  intentId: string;
  status: string;
  amount: string;
  baseCurrency: string;
  quoteCurrency: string;
  side: string;
  direction: string;
  type: string;
  price: string;
  total: string;
  externalId: string;
  network: string;
  paymentMethod: string;
  provider: string;
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
  expiresAt: Date | undefined;
}

export interface QuoteResponse {
  code: string;
  success: boolean;
  message: string;
  requestId: string;
  timestamp: Date | undefined;
  data: Quote | undefined;
}

export interface ExecuteRequest {
  quoteId: string;
  autoRequote: boolean;
  slippageBps: number;
}

export interface ExecuteResponse {
  code: string;
  success: boolean;
  message: string;
  requestId: string;
  timestamp: Date | undefined;
  data: ExecuteResponse_Data | undefined;
}

export interface ExecuteResponse_Data {
  executed: string[];
  failed: string[];
}

export interface QuoteInfoRequest {
  quoteId: string;
}

function createBaseQuoteRequest(): QuoteRequest {
  return {
    intentId: undefined,
    amount: "",
    baseCurrency: "",
    quoteCurrency: "",
    side: "",
    direction: "",
    type: undefined,
    network: "",
    paymentMethod: "",
    provider: "",
  };
}

export const QuoteRequest: MessageFns<QuoteRequest> = {
  encode(message: QuoteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.intentId !== undefined) {
      writer.uint32(10).string(message.intentId);
    }
    if (message.amount !== "") {
      writer.uint32(18).string(message.amount);
    }
    if (message.baseCurrency !== "") {
      writer.uint32(26).string(message.baseCurrency);
    }
    if (message.quoteCurrency !== "") {
      writer.uint32(34).string(message.quoteCurrency);
    }
    if (message.side !== "") {
      writer.uint32(42).string(message.side);
    }
    if (message.direction !== "") {
      writer.uint32(50).string(message.direction);
    }
    if (message.type !== undefined) {
      writer.uint32(58).string(message.type);
    }
    if (message.network !== "") {
      writer.uint32(66).string(message.network);
    }
    if (message.paymentMethod !== "") {
      writer.uint32(74).string(message.paymentMethod);
    }
    if (message.provider !== "") {
      writer.uint32(82).string(message.provider);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuoteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.intentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.baseCurrency = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.quoteCurrency = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.side = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.direction = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.network = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.paymentMethod = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuoteRequest {
    return {
      intentId: isSet(object.intentId) ? globalThis.String(object.intentId) : undefined,
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      baseCurrency: isSet(object.baseCurrency) ? globalThis.String(object.baseCurrency) : "",
      quoteCurrency: isSet(object.quoteCurrency) ? globalThis.String(object.quoteCurrency) : "",
      side: isSet(object.side) ? globalThis.String(object.side) : "",
      direction: isSet(object.direction) ? globalThis.String(object.direction) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : undefined,
      network: isSet(object.network) ? globalThis.String(object.network) : "",
      paymentMethod: isSet(object.paymentMethod) ? globalThis.String(object.paymentMethod) : "",
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
    };
  },

  toJSON(message: QuoteRequest): unknown {
    const obj: any = {};
    if (message.intentId !== undefined) {
      obj.intentId = message.intentId;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.baseCurrency !== "") {
      obj.baseCurrency = message.baseCurrency;
    }
    if (message.quoteCurrency !== "") {
      obj.quoteCurrency = message.quoteCurrency;
    }
    if (message.side !== "") {
      obj.side = message.side;
    }
    if (message.direction !== "") {
      obj.direction = message.direction;
    }
    if (message.type !== undefined) {
      obj.type = message.type;
    }
    if (message.network !== "") {
      obj.network = message.network;
    }
    if (message.paymentMethod !== "") {
      obj.paymentMethod = message.paymentMethod;
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    return obj;
  },

  create(base?: DeepPartial<QuoteRequest>): QuoteRequest {
    return QuoteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuoteRequest>): QuoteRequest {
    const message = createBaseQuoteRequest();
    message.intentId = object.intentId ?? undefined;
    message.amount = object.amount ?? "";
    message.baseCurrency = object.baseCurrency ?? "";
    message.quoteCurrency = object.quoteCurrency ?? "";
    message.side = object.side ?? "";
    message.direction = object.direction ?? "";
    message.type = object.type ?? undefined;
    message.network = object.network ?? "";
    message.paymentMethod = object.paymentMethod ?? "";
    message.provider = object.provider ?? "";
    return message;
  },
};

function createBaseQuote(): Quote {
  return {
    quoteId: "",
    intentId: "",
    status: "",
    amount: "",
    baseCurrency: "",
    quoteCurrency: "",
    side: "",
    direction: "",
    type: "",
    price: "",
    total: "",
    externalId: "",
    network: "",
    paymentMethod: "",
    provider: "",
    createdAt: undefined,
    updatedAt: undefined,
    expiresAt: undefined,
  };
}

export const Quote: MessageFns<Quote> = {
  encode(message: Quote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.quoteId !== "") {
      writer.uint32(10).string(message.quoteId);
    }
    if (message.intentId !== "") {
      writer.uint32(18).string(message.intentId);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    if (message.amount !== "") {
      writer.uint32(34).string(message.amount);
    }
    if (message.baseCurrency !== "") {
      writer.uint32(42).string(message.baseCurrency);
    }
    if (message.quoteCurrency !== "") {
      writer.uint32(50).string(message.quoteCurrency);
    }
    if (message.side !== "") {
      writer.uint32(58).string(message.side);
    }
    if (message.direction !== "") {
      writer.uint32(66).string(message.direction);
    }
    if (message.type !== "") {
      writer.uint32(74).string(message.type);
    }
    if (message.price !== "") {
      writer.uint32(82).string(message.price);
    }
    if (message.total !== "") {
      writer.uint32(90).string(message.total);
    }
    if (message.externalId !== "") {
      writer.uint32(98).string(message.externalId);
    }
    if (message.network !== "") {
      writer.uint32(106).string(message.network);
    }
    if (message.paymentMethod !== "") {
      writer.uint32(114).string(message.paymentMethod);
    }
    if (message.provider !== "") {
      writer.uint32(122).string(message.provider);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(130).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(138).fork()).join();
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(146).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Quote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quoteId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.intentId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.baseCurrency = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.quoteCurrency = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.side = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.direction = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.total = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.externalId = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.network = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.paymentMethod = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Quote {
    return {
      quoteId: isSet(object.quoteId) ? globalThis.String(object.quoteId) : "",
      intentId: isSet(object.intentId) ? globalThis.String(object.intentId) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      baseCurrency: isSet(object.baseCurrency) ? globalThis.String(object.baseCurrency) : "",
      quoteCurrency: isSet(object.quoteCurrency) ? globalThis.String(object.quoteCurrency) : "",
      side: isSet(object.side) ? globalThis.String(object.side) : "",
      direction: isSet(object.direction) ? globalThis.String(object.direction) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      total: isSet(object.total) ? globalThis.String(object.total) : "",
      externalId: isSet(object.externalId) ? globalThis.String(object.externalId) : "",
      network: isSet(object.network) ? globalThis.String(object.network) : "",
      paymentMethod: isSet(object.paymentMethod) ? globalThis.String(object.paymentMethod) : "",
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      expiresAt: isSet(object.expiresAt) ? fromJsonTimestamp(object.expiresAt) : undefined,
    };
  },

  toJSON(message: Quote): unknown {
    const obj: any = {};
    if (message.quoteId !== "") {
      obj.quoteId = message.quoteId;
    }
    if (message.intentId !== "") {
      obj.intentId = message.intentId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.baseCurrency !== "") {
      obj.baseCurrency = message.baseCurrency;
    }
    if (message.quoteCurrency !== "") {
      obj.quoteCurrency = message.quoteCurrency;
    }
    if (message.side !== "") {
      obj.side = message.side;
    }
    if (message.direction !== "") {
      obj.direction = message.direction;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.total !== "") {
      obj.total = message.total;
    }
    if (message.externalId !== "") {
      obj.externalId = message.externalId;
    }
    if (message.network !== "") {
      obj.network = message.network;
    }
    if (message.paymentMethod !== "") {
      obj.paymentMethod = message.paymentMethod;
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Quote>): Quote {
    return Quote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Quote>): Quote {
    const message = createBaseQuote();
    message.quoteId = object.quoteId ?? "";
    message.intentId = object.intentId ?? "";
    message.status = object.status ?? "";
    message.amount = object.amount ?? "";
    message.baseCurrency = object.baseCurrency ?? "";
    message.quoteCurrency = object.quoteCurrency ?? "";
    message.side = object.side ?? "";
    message.direction = object.direction ?? "";
    message.type = object.type ?? "";
    message.price = object.price ?? "";
    message.total = object.total ?? "";
    message.externalId = object.externalId ?? "";
    message.network = object.network ?? "";
    message.paymentMethod = object.paymentMethod ?? "";
    message.provider = object.provider ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.expiresAt = object.expiresAt ?? undefined;
    return message;
  },
};

function createBaseQuoteResponse(): QuoteResponse {
  return { code: "", success: false, message: "", requestId: "", timestamp: undefined, data: undefined };
}

export const QuoteResponse: MessageFns<QuoteResponse> = {
  encode(message: QuoteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.requestId !== "") {
      writer.uint32(34).string(message.requestId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(42).fork()).join();
    }
    if (message.data !== undefined) {
      Quote.encode(message.data, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuoteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuoteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.data = Quote.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuoteResponse {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      requestId: isSet(object.requestId) ? globalThis.String(object.requestId) : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      data: isSet(object.data) ? Quote.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: QuoteResponse): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.data !== undefined) {
      obj.data = Quote.toJSON(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<QuoteResponse>): QuoteResponse {
    return QuoteResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuoteResponse>): QuoteResponse {
    const message = createBaseQuoteResponse();
    message.code = object.code ?? "";
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.requestId = object.requestId ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.data = (object.data !== undefined && object.data !== null) ? Quote.fromPartial(object.data) : undefined;
    return message;
  },
};

function createBaseExecuteRequest(): ExecuteRequest {
  return { quoteId: "", autoRequote: false, slippageBps: 0 };
}

export const ExecuteRequest: MessageFns<ExecuteRequest> = {
  encode(message: ExecuteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.quoteId !== "") {
      writer.uint32(10).string(message.quoteId);
    }
    if (message.autoRequote !== false) {
      writer.uint32(16).bool(message.autoRequote);
    }
    if (message.slippageBps !== 0) {
      writer.uint32(24).int64(message.slippageBps);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quoteId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.autoRequote = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.slippageBps = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteRequest {
    return {
      quoteId: isSet(object.quoteId) ? globalThis.String(object.quoteId) : "",
      autoRequote: isSet(object.autoRequote) ? globalThis.Boolean(object.autoRequote) : false,
      slippageBps: isSet(object.slippageBps) ? globalThis.Number(object.slippageBps) : 0,
    };
  },

  toJSON(message: ExecuteRequest): unknown {
    const obj: any = {};
    if (message.quoteId !== "") {
      obj.quoteId = message.quoteId;
    }
    if (message.autoRequote !== false) {
      obj.autoRequote = message.autoRequote;
    }
    if (message.slippageBps !== 0) {
      obj.slippageBps = Math.round(message.slippageBps);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecuteRequest>): ExecuteRequest {
    return ExecuteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecuteRequest>): ExecuteRequest {
    const message = createBaseExecuteRequest();
    message.quoteId = object.quoteId ?? "";
    message.autoRequote = object.autoRequote ?? false;
    message.slippageBps = object.slippageBps ?? 0;
    return message;
  },
};

function createBaseExecuteResponse(): ExecuteResponse {
  return { code: "", success: false, message: "", requestId: "", timestamp: undefined, data: undefined };
}

export const ExecuteResponse: MessageFns<ExecuteResponse> = {
  encode(message: ExecuteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.requestId !== "") {
      writer.uint32(34).string(message.requestId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(42).fork()).join();
    }
    if (message.data !== undefined) {
      ExecuteResponse_Data.encode(message.data, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.data = ExecuteResponse_Data.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteResponse {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      requestId: isSet(object.requestId) ? globalThis.String(object.requestId) : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      data: isSet(object.data) ? ExecuteResponse_Data.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: ExecuteResponse): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.data !== undefined) {
      obj.data = ExecuteResponse_Data.toJSON(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecuteResponse>): ExecuteResponse {
    return ExecuteResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecuteResponse>): ExecuteResponse {
    const message = createBaseExecuteResponse();
    message.code = object.code ?? "";
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.requestId = object.requestId ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.data = (object.data !== undefined && object.data !== null)
      ? ExecuteResponse_Data.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseExecuteResponse_Data(): ExecuteResponse_Data {
  return { executed: [], failed: [] };
}

export const ExecuteResponse_Data: MessageFns<ExecuteResponse_Data> = {
  encode(message: ExecuteResponse_Data, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.executed) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.failed) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteResponse_Data {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteResponse_Data();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.executed.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.failed.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteResponse_Data {
    return {
      executed: globalThis.Array.isArray(object?.executed) ? object.executed.map((e: any) => globalThis.String(e)) : [],
      failed: globalThis.Array.isArray(object?.failed) ? object.failed.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: ExecuteResponse_Data): unknown {
    const obj: any = {};
    if (message.executed?.length) {
      obj.executed = message.executed;
    }
    if (message.failed?.length) {
      obj.failed = message.failed;
    }
    return obj;
  },

  create(base?: DeepPartial<ExecuteResponse_Data>): ExecuteResponse_Data {
    return ExecuteResponse_Data.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecuteResponse_Data>): ExecuteResponse_Data {
    const message = createBaseExecuteResponse_Data();
    message.executed = object.executed?.map((e) => e) || [];
    message.failed = object.failed?.map((e) => e) || [];
    return message;
  },
};

function createBaseQuoteInfoRequest(): QuoteInfoRequest {
  return { quoteId: "" };
}

export const QuoteInfoRequest: MessageFns<QuoteInfoRequest> = {
  encode(message: QuoteInfoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.quoteId !== "") {
      writer.uint32(10).string(message.quoteId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuoteInfoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuoteInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quoteId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuoteInfoRequest {
    return { quoteId: isSet(object.quoteId) ? globalThis.String(object.quoteId) : "" };
  },

  toJSON(message: QuoteInfoRequest): unknown {
    const obj: any = {};
    if (message.quoteId !== "") {
      obj.quoteId = message.quoteId;
    }
    return obj;
  },

  create(base?: DeepPartial<QuoteInfoRequest>): QuoteInfoRequest {
    return QuoteInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuoteInfoRequest>): QuoteInfoRequest {
    const message = createBaseQuoteInfoRequest();
    message.quoteId = object.quoteId ?? "";
    return message;
  },
};

export type HedgingDefinition = typeof HedgingDefinition;
export const HedgingDefinition = {
  name: "Hedging",
  fullName: "hedging.v1.Hedging",
  methods: {
    quote: {
      name: "Quote",
      requestType: QuoteRequest,
      requestStream: false,
      responseType: QuoteResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              22,
              34,
              17,
              47,
              104,
              101,
              100,
              103,
              105,
              110,
              103,
              47,
              118,
              49,
              47,
              113,
              117,
              111,
              116,
              101,
              58,
              1,
              42,
            ]),
          ],
        },
      },
    },
    execute: {
      name: "Execute",
      requestType: ExecuteRequest,
      requestStream: false,
      responseType: ExecuteResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              24,
              34,
              19,
              47,
              104,
              101,
              100,
              103,
              105,
              110,
              103,
              47,
              118,
              49,
              47,
              101,
              120,
              101,
              99,
              117,
              116,
              101,
              58,
              1,
              42,
            ]),
          ],
        },
      },
    },
    quoteInfo: {
      name: "QuoteInfo",
      requestType: QuoteInfoRequest,
      requestStream: false,
      responseType: QuoteResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              30,
              18,
              28,
              47,
              104,
              101,
              100,
              103,
              105,
              110,
              103,
              47,
              118,
              49,
              47,
              113,
              117,
              111,
              116,
              101,
              47,
              123,
              113,
              117,
              111,
              116,
              101,
              95,
              105,
              100,
              125,
            ]),
          ],
        },
      },
    },
  },
} as const;

export interface HedgingServiceImplementation<CallContextExt = {}> {
  quote(request: QuoteRequest, context: CallContext & CallContextExt): Promise<DeepPartial<QuoteResponse>>;
  execute(request: ExecuteRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ExecuteResponse>>;
  quoteInfo(request: QuoteInfoRequest, context: CallContext & CallContextExt): Promise<DeepPartial<QuoteResponse>>;
}

export interface HedgingClient<CallOptionsExt = {}> {
  quote(request: DeepPartial<QuoteRequest>, options?: CallOptions & CallOptionsExt): Promise<QuoteResponse>;
  execute(request: DeepPartial<ExecuteRequest>, options?: CallOptions & CallOptionsExt): Promise<ExecuteResponse>;
  quoteInfo(request: DeepPartial<QuoteInfoRequest>, options?: CallOptions & CallOptionsExt): Promise<QuoteResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
