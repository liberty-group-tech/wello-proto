// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.32.0
// source: wello-crypto-hub/deposit.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "wello.crypto.v1";

export enum DepositStatus {
  DEPOSIT_STATUS_UNSPECIFIED = 0,
  DEPOSIT_STATUS_PENDING = 1,
  DEPOSIT_STATUS_CONFIRMED = 2,
  DEPOSIT_STATUS_FAILED = 3,
  UNRECOGNIZED = -1,
}

export function depositStatusFromJSON(object: any): DepositStatus {
  switch (object) {
    case 0:
    case "DEPOSIT_STATUS_UNSPECIFIED":
      return DepositStatus.DEPOSIT_STATUS_UNSPECIFIED;
    case 1:
    case "DEPOSIT_STATUS_PENDING":
      return DepositStatus.DEPOSIT_STATUS_PENDING;
    case 2:
    case "DEPOSIT_STATUS_CONFIRMED":
      return DepositStatus.DEPOSIT_STATUS_CONFIRMED;
    case 3:
    case "DEPOSIT_STATUS_FAILED":
      return DepositStatus.DEPOSIT_STATUS_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DepositStatus.UNRECOGNIZED;
  }
}

export function depositStatusToJSON(object: DepositStatus): string {
  switch (object) {
    case DepositStatus.DEPOSIT_STATUS_UNSPECIFIED:
      return "DEPOSIT_STATUS_UNSPECIFIED";
    case DepositStatus.DEPOSIT_STATUS_PENDING:
      return "DEPOSIT_STATUS_PENDING";
    case DepositStatus.DEPOSIT_STATUS_CONFIRMED:
      return "DEPOSIT_STATUS_CONFIRMED";
    case DepositStatus.DEPOSIT_STATUS_FAILED:
      return "DEPOSIT_STATUS_FAILED";
    case DepositStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Deposit {
  depositId: string;
  accountId: string;
  chain: string;
  asset: string;
  txHash: string;
  amount: string;
  status: DepositStatus;
  detectedAt: Date | undefined;
  completedAt: Date | undefined;
  createdAt: Date | undefined;
}

export interface InitiateDepositRequest {
  accountId: string;
  chain: string;
  asset: string;
  amount: string;
  txHash: string;
  metadata: { [key: string]: string };
}

export interface InitiateDepositRequest_MetadataEntry {
  key: string;
  value: string;
}

export interface GetDepositRequest {
  depositId: string;
}

export interface ListDepositsRequest {
  accountIds: string[];
  chain: string;
  asset: string;
  status: DepositStatus;
  pageSize: number;
  pageToken: string;
}

export interface ListDepositsResponse {
  deposits: Deposit[];
  nextPageToken: string;
}

export interface ConfirmDepositRequest {
  depositId: string;
  confirmerId: string;
}

function createBaseDeposit(): Deposit {
  return {
    depositId: "",
    accountId: "",
    chain: "",
    asset: "",
    txHash: "",
    amount: "",
    status: 0,
    detectedAt: undefined,
    completedAt: undefined,
    createdAt: undefined,
  };
}

export const Deposit: MessageFns<Deposit> = {
  encode(message: Deposit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.depositId !== "") {
      writer.uint32(10).string(message.depositId);
    }
    if (message.accountId !== "") {
      writer.uint32(18).string(message.accountId);
    }
    if (message.chain !== "") {
      writer.uint32(26).string(message.chain);
    }
    if (message.asset !== "") {
      writer.uint32(34).string(message.asset);
    }
    if (message.txHash !== "") {
      writer.uint32(42).string(message.txHash);
    }
    if (message.amount !== "") {
      writer.uint32(50).string(message.amount);
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    if (message.detectedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.detectedAt), writer.uint32(66).fork()).join();
    }
    if (message.completedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.completedAt), writer.uint32(74).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Deposit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeposit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.depositId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.chain = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.asset = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.txHash = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.detectedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.completedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Deposit {
    return {
      depositId: isSet(object.depositId) ? globalThis.String(object.depositId) : "",
      accountId: isSet(object.accountId) ? globalThis.String(object.accountId) : "",
      chain: isSet(object.chain) ? globalThis.String(object.chain) : "",
      asset: isSet(object.asset) ? globalThis.String(object.asset) : "",
      txHash: isSet(object.txHash) ? globalThis.String(object.txHash) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      status: isSet(object.status) ? depositStatusFromJSON(object.status) : 0,
      detectedAt: isSet(object.detectedAt) ? fromJsonTimestamp(object.detectedAt) : undefined,
      completedAt: isSet(object.completedAt) ? fromJsonTimestamp(object.completedAt) : undefined,
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
    };
  },

  toJSON(message: Deposit): unknown {
    const obj: any = {};
    if (message.depositId !== "") {
      obj.depositId = message.depositId;
    }
    if (message.accountId !== "") {
      obj.accountId = message.accountId;
    }
    if (message.chain !== "") {
      obj.chain = message.chain;
    }
    if (message.asset !== "") {
      obj.asset = message.asset;
    }
    if (message.txHash !== "") {
      obj.txHash = message.txHash;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.status !== 0) {
      obj.status = depositStatusToJSON(message.status);
    }
    if (message.detectedAt !== undefined) {
      obj.detectedAt = message.detectedAt.toISOString();
    }
    if (message.completedAt !== undefined) {
      obj.completedAt = message.completedAt.toISOString();
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Deposit>): Deposit {
    return Deposit.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Deposit>): Deposit {
    const message = createBaseDeposit();
    message.depositId = object.depositId ?? "";
    message.accountId = object.accountId ?? "";
    message.chain = object.chain ?? "";
    message.asset = object.asset ?? "";
    message.txHash = object.txHash ?? "";
    message.amount = object.amount ?? "";
    message.status = object.status ?? 0;
    message.detectedAt = object.detectedAt ?? undefined;
    message.completedAt = object.completedAt ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseInitiateDepositRequest(): InitiateDepositRequest {
  return { accountId: "", chain: "", asset: "", amount: "", txHash: "", metadata: {} };
}

export const InitiateDepositRequest: MessageFns<InitiateDepositRequest> = {
  encode(message: InitiateDepositRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== "") {
      writer.uint32(10).string(message.accountId);
    }
    if (message.chain !== "") {
      writer.uint32(18).string(message.chain);
    }
    if (message.asset !== "") {
      writer.uint32(26).string(message.asset);
    }
    if (message.amount !== "") {
      writer.uint32(34).string(message.amount);
    }
    if (message.txHash !== "") {
      writer.uint32(42).string(message.txHash);
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      InitiateDepositRequest_MetadataEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitiateDepositRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitiateDepositRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.asset = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.txHash = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = InitiateDepositRequest_MetadataEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.metadata[entry6.key] = entry6.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitiateDepositRequest {
    return {
      accountId: isSet(object.accountId) ? globalThis.String(object.accountId) : "",
      chain: isSet(object.chain) ? globalThis.String(object.chain) : "",
      asset: isSet(object.asset) ? globalThis.String(object.asset) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      txHash: isSet(object.txHash) ? globalThis.String(object.txHash) : "",
      metadata: isObject(object.metadata)
        ? Object.entries(object.metadata).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: InitiateDepositRequest): unknown {
    const obj: any = {};
    if (message.accountId !== "") {
      obj.accountId = message.accountId;
    }
    if (message.chain !== "") {
      obj.chain = message.chain;
    }
    if (message.asset !== "") {
      obj.asset = message.asset;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.txHash !== "") {
      obj.txHash = message.txHash;
    }
    if (message.metadata) {
      const entries = Object.entries(message.metadata);
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<InitiateDepositRequest>): InitiateDepositRequest {
    return InitiateDepositRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<InitiateDepositRequest>): InitiateDepositRequest {
    const message = createBaseInitiateDepositRequest();
    message.accountId = object.accountId ?? "";
    message.chain = object.chain ?? "";
    message.asset = object.asset ?? "";
    message.amount = object.amount ?? "";
    message.txHash = object.txHash ?? "";
    message.metadata = Object.entries(object.metadata ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseInitiateDepositRequest_MetadataEntry(): InitiateDepositRequest_MetadataEntry {
  return { key: "", value: "" };
}

export const InitiateDepositRequest_MetadataEntry: MessageFns<InitiateDepositRequest_MetadataEntry> = {
  encode(message: InitiateDepositRequest_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitiateDepositRequest_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitiateDepositRequest_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitiateDepositRequest_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: InitiateDepositRequest_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<InitiateDepositRequest_MetadataEntry>): InitiateDepositRequest_MetadataEntry {
    return InitiateDepositRequest_MetadataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<InitiateDepositRequest_MetadataEntry>): InitiateDepositRequest_MetadataEntry {
    const message = createBaseInitiateDepositRequest_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetDepositRequest(): GetDepositRequest {
  return { depositId: "" };
}

export const GetDepositRequest: MessageFns<GetDepositRequest> = {
  encode(message: GetDepositRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.depositId !== "") {
      writer.uint32(10).string(message.depositId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDepositRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDepositRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.depositId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDepositRequest {
    return { depositId: isSet(object.depositId) ? globalThis.String(object.depositId) : "" };
  },

  toJSON(message: GetDepositRequest): unknown {
    const obj: any = {};
    if (message.depositId !== "") {
      obj.depositId = message.depositId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetDepositRequest>): GetDepositRequest {
    return GetDepositRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetDepositRequest>): GetDepositRequest {
    const message = createBaseGetDepositRequest();
    message.depositId = object.depositId ?? "";
    return message;
  },
};

function createBaseListDepositsRequest(): ListDepositsRequest {
  return { accountIds: [], chain: "", asset: "", status: 0, pageSize: 0, pageToken: "" };
}

export const ListDepositsRequest: MessageFns<ListDepositsRequest> = {
  encode(message: ListDepositsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.accountIds) {
      writer.uint32(10).string(v!);
    }
    if (message.chain !== "") {
      writer.uint32(18).string(message.chain);
    }
    if (message.asset !== "") {
      writer.uint32(26).string(message.asset);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.pageSize !== 0) {
      writer.uint32(40).int32(message.pageSize);
    }
    if (message.pageToken !== "") {
      writer.uint32(50).string(message.pageToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDepositsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDepositsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.asset = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.pageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDepositsRequest {
    return {
      accountIds: globalThis.Array.isArray(object?.accountIds)
        ? object.accountIds.map((e: any) => globalThis.String(e))
        : [],
      chain: isSet(object.chain) ? globalThis.String(object.chain) : "",
      asset: isSet(object.asset) ? globalThis.String(object.asset) : "",
      status: isSet(object.status) ? depositStatusFromJSON(object.status) : 0,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageToken: isSet(object.pageToken) ? globalThis.String(object.pageToken) : "",
    };
  },

  toJSON(message: ListDepositsRequest): unknown {
    const obj: any = {};
    if (message.accountIds?.length) {
      obj.accountIds = message.accountIds;
    }
    if (message.chain !== "") {
      obj.chain = message.chain;
    }
    if (message.asset !== "") {
      obj.asset = message.asset;
    }
    if (message.status !== 0) {
      obj.status = depositStatusToJSON(message.status);
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== "") {
      obj.pageToken = message.pageToken;
    }
    return obj;
  },

  create(base?: DeepPartial<ListDepositsRequest>): ListDepositsRequest {
    return ListDepositsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListDepositsRequest>): ListDepositsRequest {
    const message = createBaseListDepositsRequest();
    message.accountIds = object.accountIds?.map((e) => e) || [];
    message.chain = object.chain ?? "";
    message.asset = object.asset ?? "";
    message.status = object.status ?? 0;
    message.pageSize = object.pageSize ?? 0;
    message.pageToken = object.pageToken ?? "";
    return message;
  },
};

function createBaseListDepositsResponse(): ListDepositsResponse {
  return { deposits: [], nextPageToken: "" };
}

export const ListDepositsResponse: MessageFns<ListDepositsResponse> = {
  encode(message: ListDepositsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.deposits) {
      Deposit.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextPageToken !== "") {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDepositsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDepositsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deposits.push(Deposit.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextPageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDepositsResponse {
    return {
      deposits: globalThis.Array.isArray(object?.deposits) ? object.deposits.map((e: any) => Deposit.fromJSON(e)) : [],
      nextPageToken: isSet(object.nextPageToken) ? globalThis.String(object.nextPageToken) : "",
    };
  },

  toJSON(message: ListDepositsResponse): unknown {
    const obj: any = {};
    if (message.deposits?.length) {
      obj.deposits = message.deposits.map((e) => Deposit.toJSON(e));
    }
    if (message.nextPageToken !== "") {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },

  create(base?: DeepPartial<ListDepositsResponse>): ListDepositsResponse {
    return ListDepositsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListDepositsResponse>): ListDepositsResponse {
    const message = createBaseListDepositsResponse();
    message.deposits = object.deposits?.map((e) => Deposit.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? "";
    return message;
  },
};

function createBaseConfirmDepositRequest(): ConfirmDepositRequest {
  return { depositId: "", confirmerId: "" };
}

export const ConfirmDepositRequest: MessageFns<ConfirmDepositRequest> = {
  encode(message: ConfirmDepositRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.depositId !== "") {
      writer.uint32(10).string(message.depositId);
    }
    if (message.confirmerId !== "") {
      writer.uint32(18).string(message.confirmerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfirmDepositRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfirmDepositRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.depositId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.confirmerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfirmDepositRequest {
    return {
      depositId: isSet(object.depositId) ? globalThis.String(object.depositId) : "",
      confirmerId: isSet(object.confirmerId) ? globalThis.String(object.confirmerId) : "",
    };
  },

  toJSON(message: ConfirmDepositRequest): unknown {
    const obj: any = {};
    if (message.depositId !== "") {
      obj.depositId = message.depositId;
    }
    if (message.confirmerId !== "") {
      obj.confirmerId = message.confirmerId;
    }
    return obj;
  },

  create(base?: DeepPartial<ConfirmDepositRequest>): ConfirmDepositRequest {
    return ConfirmDepositRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConfirmDepositRequest>): ConfirmDepositRequest {
    const message = createBaseConfirmDepositRequest();
    message.depositId = object.depositId ?? "";
    message.confirmerId = object.confirmerId ?? "";
    return message;
  },
};

export type DepositServiceDefinition = typeof DepositServiceDefinition;
export const DepositServiceDefinition = {
  name: "DepositService",
  fullName: "wello.crypto.v1.DepositService",
  methods: {
    initiateDeposit: {
      name: "InitiateDeposit",
      requestType: InitiateDepositRequest,
      requestStream: false,
      responseType: Deposit,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [new Uint8Array([17, 34, 12, 47, 118, 49, 47, 100, 101, 112, 111, 115, 105, 116, 115, 58, 1, 42])],
        },
      },
    },
    getDeposit: {
      name: "GetDeposit",
      requestType: GetDepositRequest,
      requestStream: false,
      responseType: Deposit,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              27,
              18,
              25,
              47,
              118,
              49,
              47,
              100,
              101,
              112,
              111,
              115,
              105,
              116,
              115,
              47,
              123,
              100,
              101,
              112,
              111,
              115,
              105,
              116,
              95,
              105,
              100,
              125,
            ]),
          ],
        },
      },
    },
    listDeposits: {
      name: "ListDeposits",
      requestType: ListDepositsRequest,
      requestStream: false,
      responseType: ListDepositsResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [new Uint8Array([14, 18, 12, 47, 118, 49, 47, 100, 101, 112, 111, 115, 105, 116, 115])],
        },
      },
    },
    confirmDeposit: {
      name: "ConfirmDeposit",
      requestType: ConfirmDepositRequest,
      requestStream: false,
      responseType: Deposit,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              38,
              34,
              33,
              47,
              118,
              49,
              47,
              100,
              101,
              112,
              111,
              115,
              105,
              116,
              115,
              47,
              123,
              100,
              101,
              112,
              111,
              115,
              105,
              116,
              95,
              105,
              100,
              125,
              58,
              99,
              111,
              110,
              102,
              105,
              114,
              109,
              58,
              1,
              42,
            ]),
          ],
        },
      },
    },
  },
} as const;

export interface DepositServiceImplementation<CallContextExt = {}> {
  initiateDeposit(
    request: InitiateDepositRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<Deposit>>;
  getDeposit(request: GetDepositRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Deposit>>;
  listDeposits(
    request: ListDepositsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListDepositsResponse>>;
  confirmDeposit(request: ConfirmDepositRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Deposit>>;
}

export interface DepositServiceClient<CallOptionsExt = {}> {
  initiateDeposit(
    request: DeepPartial<InitiateDepositRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<Deposit>;
  getDeposit(request: DeepPartial<GetDepositRequest>, options?: CallOptions & CallOptionsExt): Promise<Deposit>;
  listDeposits(
    request: DeepPartial<ListDepositsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListDepositsResponse>;
  confirmDeposit(request: DeepPartial<ConfirmDepositRequest>, options?: CallOptions & CallOptionsExt): Promise<Deposit>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
