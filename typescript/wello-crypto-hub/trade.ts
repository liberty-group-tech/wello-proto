// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.32.0
// source: wello-crypto-hub/trade.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "wello.crypto.v1";

export enum OrderSide {
  ORDER_SIDE_UNSPECIFIED = 0,
  ORDER_SIDE_BUY = 1,
  ORDER_SIDE_SELL = 2,
  UNRECOGNIZED = -1,
}

export function orderSideFromJSON(object: any): OrderSide {
  switch (object) {
    case 0:
    case "ORDER_SIDE_UNSPECIFIED":
      return OrderSide.ORDER_SIDE_UNSPECIFIED;
    case 1:
    case "ORDER_SIDE_BUY":
      return OrderSide.ORDER_SIDE_BUY;
    case 2:
    case "ORDER_SIDE_SELL":
      return OrderSide.ORDER_SIDE_SELL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return OrderSide.UNRECOGNIZED;
  }
}

export function orderSideToJSON(object: OrderSide): string {
  switch (object) {
    case OrderSide.ORDER_SIDE_UNSPECIFIED:
      return "ORDER_SIDE_UNSPECIFIED";
    case OrderSide.ORDER_SIDE_BUY:
      return "ORDER_SIDE_BUY";
    case OrderSide.ORDER_SIDE_SELL:
      return "ORDER_SIDE_SELL";
    case OrderSide.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum OrderType {
  ORDER_TYPE_UNSPECIFIED = 0,
  ORDER_TYPE_MARKET = 1,
  ORDER_TYPE_LIMIT = 2,
  UNRECOGNIZED = -1,
}

export function orderTypeFromJSON(object: any): OrderType {
  switch (object) {
    case 0:
    case "ORDER_TYPE_UNSPECIFIED":
      return OrderType.ORDER_TYPE_UNSPECIFIED;
    case 1:
    case "ORDER_TYPE_MARKET":
      return OrderType.ORDER_TYPE_MARKET;
    case 2:
    case "ORDER_TYPE_LIMIT":
      return OrderType.ORDER_TYPE_LIMIT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return OrderType.UNRECOGNIZED;
  }
}

export function orderTypeToJSON(object: OrderType): string {
  switch (object) {
    case OrderType.ORDER_TYPE_UNSPECIFIED:
      return "ORDER_TYPE_UNSPECIFIED";
    case OrderType.ORDER_TYPE_MARKET:
      return "ORDER_TYPE_MARKET";
    case OrderType.ORDER_TYPE_LIMIT:
      return "ORDER_TYPE_LIMIT";
    case OrderType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum OrderStatus {
  ORDER_STATUS_UNSPECIFIED = 0,
  ORDER_STATUS_NEW = 1,
  ORDER_STATUS_PARTIALLY_FILLED = 2,
  ORDER_STATUS_FILLED = 3,
  ORDER_STATUS_CANCELLED = 4,
  ORDER_STATUS_REJECTED = 5,
  ORDER_STATUS_EXPIRED = 6,
  UNRECOGNIZED = -1,
}

export function orderStatusFromJSON(object: any): OrderStatus {
  switch (object) {
    case 0:
    case "ORDER_STATUS_UNSPECIFIED":
      return OrderStatus.ORDER_STATUS_UNSPECIFIED;
    case 1:
    case "ORDER_STATUS_NEW":
      return OrderStatus.ORDER_STATUS_NEW;
    case 2:
    case "ORDER_STATUS_PARTIALLY_FILLED":
      return OrderStatus.ORDER_STATUS_PARTIALLY_FILLED;
    case 3:
    case "ORDER_STATUS_FILLED":
      return OrderStatus.ORDER_STATUS_FILLED;
    case 4:
    case "ORDER_STATUS_CANCELLED":
      return OrderStatus.ORDER_STATUS_CANCELLED;
    case 5:
    case "ORDER_STATUS_REJECTED":
      return OrderStatus.ORDER_STATUS_REJECTED;
    case 6:
    case "ORDER_STATUS_EXPIRED":
      return OrderStatus.ORDER_STATUS_EXPIRED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return OrderStatus.UNRECOGNIZED;
  }
}

export function orderStatusToJSON(object: OrderStatus): string {
  switch (object) {
    case OrderStatus.ORDER_STATUS_UNSPECIFIED:
      return "ORDER_STATUS_UNSPECIFIED";
    case OrderStatus.ORDER_STATUS_NEW:
      return "ORDER_STATUS_NEW";
    case OrderStatus.ORDER_STATUS_PARTIALLY_FILLED:
      return "ORDER_STATUS_PARTIALLY_FILLED";
    case OrderStatus.ORDER_STATUS_FILLED:
      return "ORDER_STATUS_FILLED";
    case OrderStatus.ORDER_STATUS_CANCELLED:
      return "ORDER_STATUS_CANCELLED";
    case OrderStatus.ORDER_STATUS_REJECTED:
      return "ORDER_STATUS_REJECTED";
    case OrderStatus.ORDER_STATUS_EXPIRED:
      return "ORDER_STATUS_EXPIRED";
    case OrderStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Order {
  orderId: string;
  accountId: string;
  baseAsset: string;
  quoteAsset: string;
  side: OrderSide;
  type: OrderType;
  quantity: string;
  price: string;
  executedQuantity: string;
  status: OrderStatus;
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
  closedAt: Date | undefined;
}

export interface PlaceOrderRequest {
  accountId: string;
  baseAsset: string;
  quoteAsset: string;
  side: OrderSide;
  type: OrderType;
  quantity: string;
  price: string;
  clientOrderId: string;
  metadata: { [key: string]: string };
}

export interface PlaceOrderRequest_MetadataEntry {
  key: string;
  value: string;
}

export interface GetOrderRequest {
  orderId: string;
}

export interface ListOrdersRequest {
  accountIds: string[];
  status: OrderStatus;
  side: OrderSide;
  type: OrderType;
  pageSize: number;
  pageToken: string;
}

export interface ListOrdersResponse {
  orders: Order[];
  nextPageToken: string;
}

export interface CancelOrderRequest {
  orderId: string;
  cancelledBy: string;
  reason: string;
}

function createBaseOrder(): Order {
  return {
    orderId: "",
    accountId: "",
    baseAsset: "",
    quoteAsset: "",
    side: 0,
    type: 0,
    quantity: "",
    price: "",
    executedQuantity: "",
    status: 0,
    createdAt: undefined,
    updatedAt: undefined,
    closedAt: undefined,
  };
}

export const Order: MessageFns<Order> = {
  encode(message: Order, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderId !== "") {
      writer.uint32(10).string(message.orderId);
    }
    if (message.accountId !== "") {
      writer.uint32(18).string(message.accountId);
    }
    if (message.baseAsset !== "") {
      writer.uint32(26).string(message.baseAsset);
    }
    if (message.quoteAsset !== "") {
      writer.uint32(34).string(message.quoteAsset);
    }
    if (message.side !== 0) {
      writer.uint32(40).int32(message.side);
    }
    if (message.type !== 0) {
      writer.uint32(48).int32(message.type);
    }
    if (message.quantity !== "") {
      writer.uint32(58).string(message.quantity);
    }
    if (message.price !== "") {
      writer.uint32(66).string(message.price);
    }
    if (message.executedQuantity !== "") {
      writer.uint32(74).string(message.executedQuantity);
    }
    if (message.status !== 0) {
      writer.uint32(80).int32(message.status);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(90).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(98).fork()).join();
    }
    if (message.closedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.closedAt), writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Order {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrder();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.baseAsset = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.quoteAsset = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.quantity = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.executedQuantity = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.closedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Order {
    return {
      orderId: isSet(object.orderId) ? globalThis.String(object.orderId) : "",
      accountId: isSet(object.accountId) ? globalThis.String(object.accountId) : "",
      baseAsset: isSet(object.baseAsset) ? globalThis.String(object.baseAsset) : "",
      quoteAsset: isSet(object.quoteAsset) ? globalThis.String(object.quoteAsset) : "",
      side: isSet(object.side) ? orderSideFromJSON(object.side) : 0,
      type: isSet(object.type) ? orderTypeFromJSON(object.type) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "",
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      executedQuantity: isSet(object.executedQuantity) ? globalThis.String(object.executedQuantity) : "",
      status: isSet(object.status) ? orderStatusFromJSON(object.status) : 0,
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      closedAt: isSet(object.closedAt) ? fromJsonTimestamp(object.closedAt) : undefined,
    };
  },

  toJSON(message: Order): unknown {
    const obj: any = {};
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    if (message.accountId !== "") {
      obj.accountId = message.accountId;
    }
    if (message.baseAsset !== "") {
      obj.baseAsset = message.baseAsset;
    }
    if (message.quoteAsset !== "") {
      obj.quoteAsset = message.quoteAsset;
    }
    if (message.side !== 0) {
      obj.side = orderSideToJSON(message.side);
    }
    if (message.type !== 0) {
      obj.type = orderTypeToJSON(message.type);
    }
    if (message.quantity !== "") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.executedQuantity !== "") {
      obj.executedQuantity = message.executedQuantity;
    }
    if (message.status !== 0) {
      obj.status = orderStatusToJSON(message.status);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.closedAt !== undefined) {
      obj.closedAt = message.closedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Order>): Order {
    return Order.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Order>): Order {
    const message = createBaseOrder();
    message.orderId = object.orderId ?? "";
    message.accountId = object.accountId ?? "";
    message.baseAsset = object.baseAsset ?? "";
    message.quoteAsset = object.quoteAsset ?? "";
    message.side = object.side ?? 0;
    message.type = object.type ?? 0;
    message.quantity = object.quantity ?? "";
    message.price = object.price ?? "";
    message.executedQuantity = object.executedQuantity ?? "";
    message.status = object.status ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.closedAt = object.closedAt ?? undefined;
    return message;
  },
};

function createBasePlaceOrderRequest(): PlaceOrderRequest {
  return {
    accountId: "",
    baseAsset: "",
    quoteAsset: "",
    side: 0,
    type: 0,
    quantity: "",
    price: "",
    clientOrderId: "",
    metadata: {},
  };
}

export const PlaceOrderRequest: MessageFns<PlaceOrderRequest> = {
  encode(message: PlaceOrderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== "") {
      writer.uint32(10).string(message.accountId);
    }
    if (message.baseAsset !== "") {
      writer.uint32(18).string(message.baseAsset);
    }
    if (message.quoteAsset !== "") {
      writer.uint32(26).string(message.quoteAsset);
    }
    if (message.side !== 0) {
      writer.uint32(32).int32(message.side);
    }
    if (message.type !== 0) {
      writer.uint32(40).int32(message.type);
    }
    if (message.quantity !== "") {
      writer.uint32(50).string(message.quantity);
    }
    if (message.price !== "") {
      writer.uint32(58).string(message.price);
    }
    if (message.clientOrderId !== "") {
      writer.uint32(66).string(message.clientOrderId);
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      PlaceOrderRequest_MetadataEntry.encode({ key: key as any, value }, writer.uint32(74).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaceOrderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaceOrderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.baseAsset = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.quoteAsset = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.quantity = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.clientOrderId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          const entry9 = PlaceOrderRequest_MetadataEntry.decode(reader, reader.uint32());
          if (entry9.value !== undefined) {
            message.metadata[entry9.key] = entry9.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaceOrderRequest {
    return {
      accountId: isSet(object.accountId) ? globalThis.String(object.accountId) : "",
      baseAsset: isSet(object.baseAsset) ? globalThis.String(object.baseAsset) : "",
      quoteAsset: isSet(object.quoteAsset) ? globalThis.String(object.quoteAsset) : "",
      side: isSet(object.side) ? orderSideFromJSON(object.side) : 0,
      type: isSet(object.type) ? orderTypeFromJSON(object.type) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "",
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      clientOrderId: isSet(object.clientOrderId) ? globalThis.String(object.clientOrderId) : "",
      metadata: isObject(object.metadata)
        ? Object.entries(object.metadata).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: PlaceOrderRequest): unknown {
    const obj: any = {};
    if (message.accountId !== "") {
      obj.accountId = message.accountId;
    }
    if (message.baseAsset !== "") {
      obj.baseAsset = message.baseAsset;
    }
    if (message.quoteAsset !== "") {
      obj.quoteAsset = message.quoteAsset;
    }
    if (message.side !== 0) {
      obj.side = orderSideToJSON(message.side);
    }
    if (message.type !== 0) {
      obj.type = orderTypeToJSON(message.type);
    }
    if (message.quantity !== "") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.clientOrderId !== "") {
      obj.clientOrderId = message.clientOrderId;
    }
    if (message.metadata) {
      const entries = Object.entries(message.metadata);
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<PlaceOrderRequest>): PlaceOrderRequest {
    return PlaceOrderRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaceOrderRequest>): PlaceOrderRequest {
    const message = createBasePlaceOrderRequest();
    message.accountId = object.accountId ?? "";
    message.baseAsset = object.baseAsset ?? "";
    message.quoteAsset = object.quoteAsset ?? "";
    message.side = object.side ?? 0;
    message.type = object.type ?? 0;
    message.quantity = object.quantity ?? "";
    message.price = object.price ?? "";
    message.clientOrderId = object.clientOrderId ?? "";
    message.metadata = Object.entries(object.metadata ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBasePlaceOrderRequest_MetadataEntry(): PlaceOrderRequest_MetadataEntry {
  return { key: "", value: "" };
}

export const PlaceOrderRequest_MetadataEntry: MessageFns<PlaceOrderRequest_MetadataEntry> = {
  encode(message: PlaceOrderRequest_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaceOrderRequest_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaceOrderRequest_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaceOrderRequest_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PlaceOrderRequest_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<PlaceOrderRequest_MetadataEntry>): PlaceOrderRequest_MetadataEntry {
    return PlaceOrderRequest_MetadataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaceOrderRequest_MetadataEntry>): PlaceOrderRequest_MetadataEntry {
    const message = createBasePlaceOrderRequest_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetOrderRequest(): GetOrderRequest {
  return { orderId: "" };
}

export const GetOrderRequest: MessageFns<GetOrderRequest> = {
  encode(message: GetOrderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderId !== "") {
      writer.uint32(10).string(message.orderId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOrderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOrderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOrderRequest {
    return { orderId: isSet(object.orderId) ? globalThis.String(object.orderId) : "" };
  },

  toJSON(message: GetOrderRequest): unknown {
    const obj: any = {};
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetOrderRequest>): GetOrderRequest {
    return GetOrderRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetOrderRequest>): GetOrderRequest {
    const message = createBaseGetOrderRequest();
    message.orderId = object.orderId ?? "";
    return message;
  },
};

function createBaseListOrdersRequest(): ListOrdersRequest {
  return { accountIds: [], status: 0, side: 0, type: 0, pageSize: 0, pageToken: "" };
}

export const ListOrdersRequest: MessageFns<ListOrdersRequest> = {
  encode(message: ListOrdersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.accountIds) {
      writer.uint32(10).string(v!);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.side !== 0) {
      writer.uint32(24).int32(message.side);
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    if (message.pageSize !== 0) {
      writer.uint32(40).int32(message.pageSize);
    }
    if (message.pageToken !== "") {
      writer.uint32(50).string(message.pageToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListOrdersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListOrdersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.pageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListOrdersRequest {
    return {
      accountIds: globalThis.Array.isArray(object?.accountIds)
        ? object.accountIds.map((e: any) => globalThis.String(e))
        : [],
      status: isSet(object.status) ? orderStatusFromJSON(object.status) : 0,
      side: isSet(object.side) ? orderSideFromJSON(object.side) : 0,
      type: isSet(object.type) ? orderTypeFromJSON(object.type) : 0,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageToken: isSet(object.pageToken) ? globalThis.String(object.pageToken) : "",
    };
  },

  toJSON(message: ListOrdersRequest): unknown {
    const obj: any = {};
    if (message.accountIds?.length) {
      obj.accountIds = message.accountIds;
    }
    if (message.status !== 0) {
      obj.status = orderStatusToJSON(message.status);
    }
    if (message.side !== 0) {
      obj.side = orderSideToJSON(message.side);
    }
    if (message.type !== 0) {
      obj.type = orderTypeToJSON(message.type);
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== "") {
      obj.pageToken = message.pageToken;
    }
    return obj;
  },

  create(base?: DeepPartial<ListOrdersRequest>): ListOrdersRequest {
    return ListOrdersRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListOrdersRequest>): ListOrdersRequest {
    const message = createBaseListOrdersRequest();
    message.accountIds = object.accountIds?.map((e) => e) || [];
    message.status = object.status ?? 0;
    message.side = object.side ?? 0;
    message.type = object.type ?? 0;
    message.pageSize = object.pageSize ?? 0;
    message.pageToken = object.pageToken ?? "";
    return message;
  },
};

function createBaseListOrdersResponse(): ListOrdersResponse {
  return { orders: [], nextPageToken: "" };
}

export const ListOrdersResponse: MessageFns<ListOrdersResponse> = {
  encode(message: ListOrdersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.orders) {
      Order.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextPageToken !== "") {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListOrdersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListOrdersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orders.push(Order.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextPageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListOrdersResponse {
    return {
      orders: globalThis.Array.isArray(object?.orders) ? object.orders.map((e: any) => Order.fromJSON(e)) : [],
      nextPageToken: isSet(object.nextPageToken) ? globalThis.String(object.nextPageToken) : "",
    };
  },

  toJSON(message: ListOrdersResponse): unknown {
    const obj: any = {};
    if (message.orders?.length) {
      obj.orders = message.orders.map((e) => Order.toJSON(e));
    }
    if (message.nextPageToken !== "") {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },

  create(base?: DeepPartial<ListOrdersResponse>): ListOrdersResponse {
    return ListOrdersResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListOrdersResponse>): ListOrdersResponse {
    const message = createBaseListOrdersResponse();
    message.orders = object.orders?.map((e) => Order.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? "";
    return message;
  },
};

function createBaseCancelOrderRequest(): CancelOrderRequest {
  return { orderId: "", cancelledBy: "", reason: "" };
}

export const CancelOrderRequest: MessageFns<CancelOrderRequest> = {
  encode(message: CancelOrderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderId !== "") {
      writer.uint32(10).string(message.orderId);
    }
    if (message.cancelledBy !== "") {
      writer.uint32(18).string(message.cancelledBy);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelOrderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelOrderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cancelledBy = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelOrderRequest {
    return {
      orderId: isSet(object.orderId) ? globalThis.String(object.orderId) : "",
      cancelledBy: isSet(object.cancelledBy) ? globalThis.String(object.cancelledBy) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: CancelOrderRequest): unknown {
    const obj: any = {};
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    if (message.cancelledBy !== "") {
      obj.cancelledBy = message.cancelledBy;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<CancelOrderRequest>): CancelOrderRequest {
    return CancelOrderRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CancelOrderRequest>): CancelOrderRequest {
    const message = createBaseCancelOrderRequest();
    message.orderId = object.orderId ?? "";
    message.cancelledBy = object.cancelledBy ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

export type TradeServiceDefinition = typeof TradeServiceDefinition;
export const TradeServiceDefinition = {
  name: "TradeService",
  fullName: "wello.crypto.v1.TradeService",
  methods: {
    placeOrder: {
      name: "PlaceOrder",
      requestType: PlaceOrderRequest,
      requestStream: false,
      responseType: Order,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [new Uint8Array([15, 34, 10, 47, 118, 49, 47, 111, 114, 100, 101, 114, 115, 58, 1, 42])],
        },
      },
    },
    getOrder: {
      name: "GetOrder",
      requestType: GetOrderRequest,
      requestStream: false,
      responseType: Order,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              23,
              18,
              21,
              47,
              118,
              49,
              47,
              111,
              114,
              100,
              101,
              114,
              115,
              47,
              123,
              111,
              114,
              100,
              101,
              114,
              95,
              105,
              100,
              125,
            ]),
          ],
        },
      },
    },
    listOrders: {
      name: "ListOrders",
      requestType: ListOrdersRequest,
      requestStream: false,
      responseType: ListOrdersResponse,
      responseStream: false,
      options: {
        _unknownFields: { 578365826: [new Uint8Array([12, 18, 10, 47, 118, 49, 47, 111, 114, 100, 101, 114, 115])] },
      },
    },
    cancelOrder: {
      name: "CancelOrder",
      requestType: CancelOrderRequest,
      requestStream: false,
      responseType: Order,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              33,
              34,
              28,
              47,
              118,
              49,
              47,
              111,
              114,
              100,
              101,
              114,
              115,
              47,
              123,
              111,
              114,
              100,
              101,
              114,
              95,
              105,
              100,
              125,
              58,
              99,
              97,
              110,
              99,
              101,
              108,
              58,
              1,
              42,
            ]),
          ],
        },
      },
    },
  },
} as const;

export interface TradeServiceImplementation<CallContextExt = {}> {
  placeOrder(request: PlaceOrderRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Order>>;
  getOrder(request: GetOrderRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Order>>;
  listOrders(
    request: ListOrdersRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListOrdersResponse>>;
  cancelOrder(request: CancelOrderRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Order>>;
}

export interface TradeServiceClient<CallOptionsExt = {}> {
  placeOrder(request: DeepPartial<PlaceOrderRequest>, options?: CallOptions & CallOptionsExt): Promise<Order>;
  getOrder(request: DeepPartial<GetOrderRequest>, options?: CallOptions & CallOptionsExt): Promise<Order>;
  listOrders(
    request: DeepPartial<ListOrdersRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListOrdersResponse>;
  cancelOrder(request: DeepPartial<CancelOrderRequest>, options?: CallOptions & CallOptionsExt): Promise<Order>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
